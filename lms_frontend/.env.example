# Frontend Environment Variables (Vite-style for Supabase)
# IMPORTANT: Do not commit real values.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: Explicit site URL for auth redirects (otherwise window.location.origin is used)
VITE_SITE_URL=
