# Create React App environment variables for Ocean LMS frontend
# Required (client-safe; NEVER use service_role on the frontend):
REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-public-key

# Optional: Explicit base URL for building auth redirect links.
# If not set, window.location.origin is used.
# REACT_APP_FRONTEND_URL=http://localhost:3000
